package mutation

const (
	mutation_prompt string = "Take the following in resolution message %s and apply it to this manifest %s and respond with just the new manifest as a string without yaml or backticks around it."
)
